# Core Application Settings 
default:
  admin: 'instructor@utoronto.ca' # Main Instructor Email
  appPort: 5000 # Port the application will run on
  dbServer: 'http://127.0.0.1'  # CouchDB Database Server IP
  dbPort: 5984 # CouchDB Database Server Port
